import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faShoppingCart } from "@fortawesome/free-solid-svg-icons";

function CartWidget() {
  return <FontAwesomeIcon icon={faShoppingCart} className="ms-3" />;
}

export default CartWidget;
